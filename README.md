# CyberAscii Vision

A real-time webcam to ASCII art generator.

Created by Gourab.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
